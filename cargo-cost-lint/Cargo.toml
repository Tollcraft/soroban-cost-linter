[package]
name = "cargo-cost-lint"
version = "0.1.1"
edition = "2024"
license = "Apache-2.0"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "0.8"
clap = { version = "4.0", features = ["derive"] }
ignore = "0.4"

[dev-dependencies]
tempfile = "3"
